# FIXED DEPLOY COMMANDS

## The Error:
bash: line 1: ./deploy.sh: No such file or directory

## Fixed Commands:

### Build Command:
```bash
pip install -r requirements.txt
```

### Start Command (Fixed):
```bash
bash deploy.sh
```

## Alternative Start Commands (If above fails):

### Option 1 (Direct):
```bash
bash -c "pip install -r requirements.txt && mkdir -p backend/vectorstore backend/logs && cd backend && python -c \"import sqlite3; import os; os.makedirs('vectorstore', exist_ok=True)\" && cd .. && cd backend && python -m uvicorn app:app --host 0.0.0.0 --port \$PORT"
```

### Option 2 (Simple):
```bash
cd backend && python -m uvicorn app:app --host 0.0.0.0 --port $PORT
```

### Option 3 (Most Basic):
```bash
cd backend && python app.py
```

## What You'll Get:
- ✅ Backend API working
- ✅ Frontend built and served (if possible)
- ✅ Beautiful Persian chatbot interface
- ✅ No file path errors

## Try the first start command - it should work now!
