# FIXED UVICORN COMMANDS

## The Error:
ERROR: Error loading ASGI app. Import string "app:" must be in format "<module>:<attribute>".

## Fixed Commands:

### Build Command:
```bash
pip install -r requirements.txt && mkdir -p backend/vectorstore backend/logs && cd backend && python -c "import sqlite3; import os; os.makedirs('vectorstore', exist_ok=True)" && cd ..
```

### Start Command (Fixed):
```bash
cd backend && python -m uvicorn app:app --host 0.0.0.0 --port $PORT
```

## Alternative Start Commands (If above fails):

### Option 1:
```bash
cd backend && python -m uvicorn app:app --host 0.0.0.0 --port $PORT --workers 1
```

### Option 2:
```bash
cd backend && python app.py
```

### Option 3:
```bash
cd backend && python -c "import uvicorn; from app import app; uvicorn.run(app, host='0.0.0.0', port=int('$PORT'))"
```

## What You'll Get:
- ✅ Beautiful Persian chatbot interface at root URL
- ✅ Backend API at /api
- ✅ Chat functionality working
- ✅ No import errors

## Try the first start command - it should work now!
